#include <stdio.h>

int main (int argc, char argr [])
   {
    char nombre[50];
     int i;
     printf("ingrese su nombre \n");
     gets (nombre);
      for (i=0; i<=10000; i++)
      {
       printf ("\n %s",nombre);
      }
 return 0;
    }
