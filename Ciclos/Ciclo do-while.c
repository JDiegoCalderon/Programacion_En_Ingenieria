#include<stdio.h>


int main(int argc,const char*argr)
{
    int i, n, sg, num, den, x;
    float ln;

        printf("Ingrese el numero de elementos: ");
        scanf("%d",&n);
    for(i=0; ln=0; i++, x=2);


        ln x= sg*num/den;
    printf("ln= %lf", x);
    system("PAUSE");
    return 0;
}

